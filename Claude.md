# Instructions for Claude:
- Follow the Django MVC file structure (database structure in models, frontend page handled in templates and static, and controller in views, etc)
- When writing design md files, don't make things too verbose. We should show some code snippets or text-based data flow diagrams/tables, but the descriptions should be in a way that non-programmers can also understand concisely.
- For language just follow the format of Django (Python for backend, HTML/CSS/JS for frontend)
- When we are updating any of the code and there are major changes, add it to the design file as well