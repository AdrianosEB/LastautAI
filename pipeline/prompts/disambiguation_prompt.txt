The following fragments from a workflow description are ambiguous. For each fragment, provide possible interpretations with confidence scores.

Ambiguous fragments:
{fragments}

For each fragment, return a JSON array of objects with this structure:
{
  "fragment": "string — the ambiguous text",
  "interpretations": [
    {
      "text": "string — one possible interpretation",
      "confidence": 0.0 to 1.0
    }
  ]
}

Return ONLY valid JSON. No markdown, no explanation.